# 智能拦截项目 · 架构脉络与推进清单

> 只写骨架。每推进一步边做边改，本文件在迭代中持续更新。

## 项目定位

视觉主机负责"球会落在哪"的预测与决策，下位机只负责"把人送过去"。视觉部门包圆智能，Arduino 只执行。

## 系统架构（主机 + 小车两个节点）

```
[双USB相机 L/R]  ──同步帧对──▶ [检测层] YOLO(主)+HSV(兜底)  ──/ball_pix_L,R──▶
[定位层] 双目标定+三角化 ──/ball_3d(相机系)──▶ [预测/决策层] 物理拟合(带阻力) ──/interception_goal(X,Y,t) 世界系
        ──▶ [通信层] tf坐标系变换 ──rosserial/串口──▶ [Arduino UNO R3] 麦轮运动学+PID(编码器拓展板) ──▶ 四轮到位──▶ 钢板接球
```

- **主机（上位机）**：Ubuntu 20.04 + ROS Noetic。五个逻辑层：采集 → 检测 → 定位 → 预测决策 → 通信。
- **小车（下位机）**：Arduino UNO R3 + 电机控制拓展板。只做一个闭环：收到目标点 → 麦轮运动学 → PID 到位。

## 数据流脉络（一条线贯穿）

```
帧对 → 双目光路像素 → 球3D点序列 → 轨迹拟合 → 拦截点(X,Y,t)
     → 单次目标点下发 → 底盘到位 → 钢板接住下坠的球
```

## 坐标系脉络

```
相机系(cam) ──▶ 地面/世界系(map) ──▶ 底盘系(robot)
```
MVP 场地固定，用固定外参把拦截点映射到地面系；小车端只认自己的平面坐标。

## 里程碑推进清单

- [x] **M0 环境就绪**：Noetic、双 USB 相机架设（宽基线）、catkin 工作区 + rosserial。验收：能同时采到两路帧。✅ 2026-09-10：双路 MJPG 640x480@30 稳定出帧（`/cam_left/image_raw`、`/cam_right/image_raw`，节点 `smart_interception_camera`）。
- [ ] **M1 棋盘立体标定**：内参、基线、校正映射。验收：重投影误差达标。
- [ ] **M2 检测**：先 HSV 跑通链路，YOLO 小数据训练后接管为主、HSV 兜底。验收：左右目都稳定出球心。
- [ ] **M3 三角化 3D 定位**。验收：静态球 2/3/4m 误差在预算内。
- [ ] **M4 物理拟合 + 拦截点求解**。验收：录的抛球离线回放，预测 vs 实际偏移可接受。
- [ ] **M5 Arduino 点状到位**。验收：独立路点测试，小车到达指定 2D 点误差小。
- [ ] **M6 端到端联调**：投球 → 小车接住。验收：接球成功率到起步线。

后置阶段（拿到验证数据后再谈）：**预测核心 NN 化**（仿真生成标注轨迹训练回归网络），热插拔替换 M4 的物理拟合。

## 设计支柱

1. **一次锤定一个拦截目标**，不逐帧追预测点 → 消除"追下一状态"的闭环焦虑、避免误差放大。
2. **物理先行 → NN 后置**：物理版先证明全链路可行，再换 NN 升维；两头都不裸奔。
3. **钢板接球 = 2D 地面问题**：把空中接球简化为"钢板平移到落点正下方"。
4. **宽基线独立双 USB 相机**：3–5m 深度误差压到厘米级。
5. **YOLO + HSV 协同**：HSV 兜底/验证，且用 HSV 自动给 YOLO 生成标注数据，加速 DL 数据管线。

## 决策记录（迭代追加）

- **2026-09-08**：球为单一橙色乒乓球；双目用两颗独立 USB 相机宽基线；检测 YOLO(主)+HSV(兜底)；预测物理先行、NN(仿真训练)后置热插拔；拦截=车顶钢板接下坠球（2D 问题）；抛距 3–5m；底盘 TT 编码器电机+四驱麦轮+电机拓展板；上位机与小车为两个 ROS Noetic 通信节点(rosserial)。
- **2026-09-10**：M0 落地——容器挂载项目 `catkin_ws`，`/dev/video*` 透传进容器（compose `devices` + `group_add` video gid）；相机映射 video2=左(HD 720P Webcam)、video4=右(USB Camera)，`usb_cam` 双路 640x480 MJPG@30，话题 `cam_left|right/image_raw`（usb_cam 话题在节点命名空间下）；launch 包 `smart_interception_camera`。
- **2026-09-13**：M1 标定踩坑与决策——① `/dev/videoN` 编号随插拔漂移，长期容器内节点为启动时旧映射（残留），**插拔相机后必须重启容器**才能挂载新设备；② camera_calibration 双目命令必须用 `left:=`/`left_camera:=`/`right:=`/`right_camera:=`（单目 `image:`/`camera:` 语法会使双目退化成单目、只存 left 图），非同步双 usb_cam 需加 `--no-service-check --approximate=...`；③ 单目标定保存图片固定命名 `left-*.png` 属正常，判断成功看 `ost.yaml` 是否有参数；④ 容器里 `python` 不存在，用 `python3`。
- **2026-09-13**：M1 双目立体标定完成。最终方案：`scripts/stereo_calib.py`——OpenCV 标定 + `message_filters` 同步（slop=0.05s）+ **停稳0.6s自动采集**（移动中不采）；标定格式 YUYV 1280x720@10。结果：**RMS 0.99px、基线 6.44cm**（40 对图存 `/catkin_ws/calib_pairs`）。左右映射最终确认：`/dev/video2`=HD 720P=cam_left（物理左）、`/dev/video4`=USB Camera=cam_right（物理右）；标定采集时映射未重启导致左右颠倒，用 `fix_swap.py` 交换左右重算矫正矩阵修复（无需重采）。误差根因复盘：**左右帧不同步 + 移动中采帧**是主因（`--approximate=0.1` 在 10fps 下可差整帧），MJPG 压缩和 640 低分辨率是放大器。部署：`config/cam_{left,right}_camera_info.yaml` + launch `camera_info_url`；运行默认 MJPG 1280x720@30（内参只绑分辨率不绑压缩格式）。⚠ 相机物理位置锁死不可再动。
- **2026-09-15**:M2 双目拉流实时颜色阈值检测+点迹绘制完成；新增视频检测（单帧检测、环形buffer）；重构项目为C++实现，为后续拓展奠定基础；优化项目结构（common.h)；对后续路线进行了探讨和修正。
- **2026-09-16**：补充关键注释便于维护；
